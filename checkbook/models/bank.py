# coding: utf-8

"""
    Checkbook API

    The Checkbook API enables businesses to programmatically send and receive payments using digital checks and other payment methods. With Checkbook, you can automate payment workflows, disburse funds instantly, and manage transactions securely—all without the delays and costs of traditional paper checks.

    The version of the OpenAPI document: 3.0.0
    Contact: support@checkbook.io
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self

class Bank(BaseModel):
    """
    Bank
    """ # noqa: E501
    account: StrictStr = Field(description="Last 4 of account number")
    balance: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Indicates the current amount left in the account's balance (only for prefunded accounts)")
    billing: StrictBool = Field(description="Indicates the billing account for the user")
    var_date: StrictStr = Field(description="Account creation timestamp", alias="date")
    default: StrictBool = Field(description="Indicates the default account for the user")
    id: StrictStr = Field(description="Unique identifier for account")
    name: Optional[StrictStr] = Field(default=None, description="Name of the bank account")
    routing: StrictStr = Field(description="Routing number")
    status: StrictStr = Field(description="Current status of account")
    type: StrictStr = Field(description="Bank account type")
    __properties: ClassVar[List[str]] = ["account", "balance", "billing", "date", "default", "id", "name", "routing", "status", "type"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['PENDING', 'VERIFIED', 'DEPOSIT_ONLY']):
            raise ValueError("must be one of enum values ('PENDING', 'VERIFIED', 'DEPOSIT_ONLY')")
        return value

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['CHECKING', 'SAVINGS', 'BUSINESS']):
            raise ValueError("must be one of enum values ('CHECKING', 'SAVINGS', 'BUSINESS')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Bank from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if balance (nullable) is None
        # and model_fields_set contains the field
        if self.balance is None and "balance" in self.model_fields_set:
            _dict['balance'] = None

        # set to None if name (nullable) is None
        # and model_fields_set contains the field
        if self.name is None and "name" in self.model_fields_set:
            _dict['name'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Bank from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "account": obj.get("account"),
            "balance": obj.get("balance"),
            "billing": obj.get("billing"),
            "date": obj.get("date"),
            "default": obj.get("default"),
            "id": obj.get("id"),
            "name": obj.get("name"),
            "routing": obj.get("routing"),
            "status": obj.get("status"),
            "type": obj.get("type")
        })
        return _obj


